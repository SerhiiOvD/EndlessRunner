VisualElement {
}

.root {
    opacity: 1;
    display: flex;
    visibility: visible;
    flex-shrink: 0;
    flex-grow: 1;
    background-color: rgb(224, 0, 0);
}

.unity-progress-bar {
    display: flex;
}

.unity-progress-bar__container {
}
